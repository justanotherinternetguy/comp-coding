#include <bits/stdc++.h>
using namespace std;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // freopen("name.in", "r", stdin);
    // freopen("name.out", "w", stdout);

    string s;
    cin >> s;
    cout << s << '\n';

    return 0;
}